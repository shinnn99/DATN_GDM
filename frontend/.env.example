# Không bắt buộc — frontend gọi `/api/*` và Vercel sẽ rewrite sang backend
# theo cấu hình trong vercel.json. File này chỉ để tham khảo nếu sau này
# muốn chuyển sang gọi backend trực tiếp (cross-origin) qua biến môi trường.

# VITE_API_BASE_URL=https://your-backend.onrender.com
